{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "npx lerna exec -- yarn lint",
    "dev:coffee": "npx lerna exec --scope=discover-coffee-stores -- yarn dev",
    "build:coffee": "npx lerna exec --scope=discover-coffee-stores -- yarn build",
    "start:coffee": "npx lerna exec --scope=discover-coffee-stores -- yarn start"
  },
  "pre-commit": [
    "lint"
  ],
  "devDependencies": {
    "lerna": "^5.1.1",
    "pre-commit": "^1.2.2"
  }
}